name: wayfind_server
description: Local REST + WebSocket server for WayFind (in-memory)
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^1.0.4
  web_socket_channel: ^2.4.5
  uuid: ^4.5.1
